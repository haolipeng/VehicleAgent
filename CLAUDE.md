# 项目需求：轻量级客户端探针（Agent）框架

## 项目概述

基于 fluent-bit 精简裁剪，实现一个轻量级客户端探针（Agent）框架，运行在 ARM 架构的嵌入式/车载 Linux 环境中。

## 核心需求

### 运行环境
- **目标架构**: ARM（嵌入式/车载 Linux）
- **开发语言**: C
- **运行方式**: daemon 长期运行
- **资源要求**: 低资源占用、高稳定性

### 核心职责
- 作为**数据承载与传输框架**
- 通过 **TCP 长连接**将数据稳定、可靠地发送至服务端
- 必要时可扩展为 HTTP 传输（**不使用 MQTT**）

### 数据来源
- 主要来源：客户环境提供的接口（外部数据源）
- 可选功能：系统资源采集（CPU、内存等基础信息），非必选

### 设计原则
- **低资源占用**: 适合嵌入式环境，内存和 CPU 占用最小化
- **高稳定性**: daemon 长期运行，连接断开自动重连
- **模块化结构**: 便于裁剪、移植和二次开发
- **可扩展性**: 数据采集和传输协议均可扩展

## 已完成的裁剪工作

基于 fluent-bit v4.2.2 分支进行精简：

1. ~~Kafka 插件~~ - 已删除
2. ~~Lua 脚本支持~~ - 已删除
3. ~~WASM 支持~~ - 已删除
4. ~~容器化部署文件~~ - 已删除
5. ~~Stream Processor~~ - 已完全移除（源码、头文件、CMake、配置解析、测试）
6. **Multiline** - 已用 stub 替代
7. **Record Accessor** - 已用 stub 替代
8. CMake 路径修复：jansson 条件检测、nghttp2 路径修正

## 构建说明

```bash
cd build
cmake ..
make -j4
./bin/fluent-bit --version
```

## 分支信息

- 当前分支: `v4.2.2`
- 基础版本: fluent-bit v4.2.2
